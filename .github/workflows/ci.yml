name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run tests
      run: npm test
    
    - name: Verify CLI functionality
      run: |
        npm start example
        npm start -- --help
    
    - name: Check for security vulnerabilities
      run: npm audit --audit-level moderate

  build:
    needs: test
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Use Node.js 18
      uses: actions/setup-node@v4
      with:
        node-version: 18
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build verification
      run: npm run build
    
    - name: Run submission verification
      run: node verify-submission.js

  deploy-status:
    needs: [test, build]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deployment success notification
      run: |
        echo "✅ All checks passed!"
        echo "🚀 Application is ready for production deployment"
        echo "📊 Netlify deployment: https://flourishing-licorice-5dc1a6.netlify.app"
        echo "📡 API endpoint: https://flourishing-licorice-5dc1a6.netlify.app/.netlify/functions/research"